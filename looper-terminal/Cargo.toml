[package]
name = "looper-terminal"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0"
looper-harness = { path = "../looper-harness" }
axum = "0.8"
tokio = { version = "=1.48.0", features = ["macros", "rt-multi-thread", "net"] }
